#pragma once

namespace Graphene {

#define INLINE inline constexpr
#define CONST  const constexpr

using i8  = char;
using i16 = short;
using i32 = int;
using i64 = long long;

using u8  = unsigned char;
using u16 = unsigned short;
using u32 = unsigned int;
using u64 = unsigned long long;

using Move = int;
using MS = u64; // milliseconds

}
